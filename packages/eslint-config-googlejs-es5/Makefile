MOCHA := node_modules/mocha/bin/mocha --no-colors

test:
	npm test

develop:
	npm link eslint-plugin-googlejs
	npm link eslint-config-tester
	npm link eslint-config-googlejs-base
	npm link
	npm install
